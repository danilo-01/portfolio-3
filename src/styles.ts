export const primaryColor = "#1E1E1E";

export const fadeInTransition = ` @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  & {
    animation: fadeIn 0.5s ease-in-out;
  }`;
